# Plan: 01-03 - Data Loaders & Split

**Objective:** Implement data splitting logic and PyTorch Geometric data loaders.

---

## Frontmatter
- **Wave:** 3
- **Depends on:** 01-02
- **Files Modified:** 
  - `projects/src/data/loader.py`
  - `projects/src/main.py`
- **Autonomous:** true
- **Requirements Addressed:** INFRA-04

---

## Tasks

<task>
<objective>Implement the data split logic.</objective>
<read_first>
- projects/Pattern_Project_Elliptic_dataset_GCN.ipynb
</read_first>
<acceptance_criteria>
- `projects/src/data/loader.py` exists.
- Split logic uses the 34/15 timestep split (first 34 for training, last 15 for testing).
</acceptance_criteria>
<action>
Implement a function in `loader.py` that takes the `Data` object and returns `train_loader` and `test_loader`.
Ensure the split is based on the `time step` feature (index 1 of features before normalization or a separate array).
</action>
</task>

<task>
<objective>Implement the main entry point.</objective>
<read_first>
- .planning/phases/01-infrastructure-modularization/01-CONTEXT.md
</read_first>
<acceptance_criteria>
- `projects/src/main.py` exists.
- Script orchestrates downloading, dataset creation, and loader initialization.
- Script prints dataset statistics (nodes, edges, illicit class ratio).
</acceptance_criteria>
<action>
Create `src/main.py`. 
Import `downloader`, `EllipticDataset`, and the loader functions.
Add a `--download` flag to trigger the `downloader.py` logic.
</action>
</task>

<task>
<objective>Create placeholder for models.</objective>
<read_first>
- .planning/phases/01-infrastructure-modularization/01-CONTEXT.md
</read_first>
<acceptance_criteria>
- `projects/src/models/gcn.py` and `projects/src/models/evolve_gcn.py` exist as placeholders.
</acceptance_criteria>
<action>
Create empty `projects/src/models/gcn.py` and `projects/src/models/evolve_gcn.py` to satisfy the directory structure requirements.
</action>
</task>

---

## Verification
- Run `python projects/src/main.py` and verify it prints the correct statistics and initializes loaders.
