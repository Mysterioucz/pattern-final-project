---
phase: 01-infrastructure-modularization
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - projects/src/main.py
  - pyproject.toml
autonomous: true
gap_closure: true
requirements:
  - INFRA-02

must_haves:
  truths:
    - "main.py --download flag imports and calls download_elliptic_dataset without ImportError or TypeError"
    - "python-dotenv and kaggle packages are declared in pyproject.toml dependencies"
  artifacts:
    - path: "projects/src/main.py"
      provides: "_download() function with correct import and kwarg"
      contains: "download_elliptic_dataset"
    - path: "pyproject.toml"
      provides: "project dependencies include python-dotenv and kaggle"
      contains: "python-dotenv"
  key_links:
    - from: "projects/src/main.py _download()"
      to: "projects/src/data/downloader.py"
      via: "from data.downloader import download_elliptic_dataset"
      pattern: "download_elliptic_dataset"
    - from: "projects/src/main.py _download()"
      to: "download_elliptic_dataset function"
      via: "dest_dir= kwarg"
      pattern: "dest_dir="
---

<objective>
Fix the broken `--download` CLI path in `main.py` (CR-01 and CR-02) and add missing
package dependencies to `pyproject.toml`.

Purpose: `python projects/src/main.py --download` currently crashes with an
`ImportError` on the first line of `_download()`. Two bugs compound: the imported
function name is wrong (`download_dataset` instead of `download_elliptic_dataset`) and
the keyword argument passed is wrong (`dest=` instead of `dest_dir=`). Additionally,
`python-dotenv` and `kaggle` are absent from `pyproject.toml`, so the downloader cannot
be installed in a clean environment.

Output: A working `--download` CLI path and a correctly declared dependency list.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-modularization/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix _download() in main.py (CR-01 + CR-02)</name>
  <files>projects/src/main.py</files>

  <read_first>projects/src/main.py</read_first>

  <action>
Edit `projects/src/main.py`, function `_download()` (lines 36–42).

Make exactly two changes:

1. Line 38 — change the import name from `download_dataset` to `download_elliptic_dataset`:

  BEFORE:
    from data.downloader import download_dataset  # type: ignore[import]

  AFTER:
    from data.downloader import download_elliptic_dataset  # type: ignore[import]

2. Lines 40–41 — change the function call to use the correct name and kwarg:

  BEFORE:
    download_dataset(dest=str(_PROJECTS_DIR / "data" / "raw"), force=False)

  AFTER:
    download_elliptic_dataset(dest_dir=str(_PROJECTS_DIR / "data" / "raw"), force=False)

No other lines in the file should be touched. The rest of _download() (the two print
statements and the function signature) stays exactly as-is.

Verify the function signature in downloader.py if needed: `download_elliptic_dataset(dest_dir, force)`.
  </action>

  <verify>
    <automated>python -c "
import ast, sys
src = open('projects/src/main.py').read()
tree = ast.parse(src)
body = src
assert 'download_elliptic_dataset' in body, 'FAIL: old name still present'
assert 'dest_dir=' in body, 'FAIL: wrong kwarg still present'
assert 'download_dataset' not in body.replace('download_elliptic_dataset', ''), 'FAIL: old function name remains'
assert 'dest=' not in body.replace('dest_dir=', ''), 'FAIL: old kwarg remains'
print('PASS: main.py has correct import name and kwarg')
"
    </automated>
  </verify>

  <acceptance_criteria>
- `grep -n "download_elliptic_dataset" projects/src/main.py` returns lines 38 and 41
- `grep -n "download_dataset[^_]" projects/src/main.py` returns no results (old name gone)
- `grep -n "dest_dir=" projects/src/main.py` returns line 41
- `grep -n "dest=" projects/src/main.py` that is NOT `dest_dir=` returns no results
  </acceptance_criteria>

  <done>main.py _download() calls download_elliptic_dataset(dest_dir=...) — no ImportError or TypeError on --download flag</done>
</task>

<task type="auto">
  <name>Task 2: Add python-dotenv and kaggle to pyproject.toml</name>
  <files>pyproject.toml</files>

  <read_first>pyproject.toml</read_first>

  <action>
Edit `pyproject.toml` (root of repo, NOT inside projects/). Add two new entries to the
`dependencies` list inside `[project]`:

  "python-dotenv>=1.0.0",
  "kaggle>=1.6.0",

Insert them after the existing `"pandas>=3.0.1",` line. The full updated dependencies
block should be:

    dependencies = [
        "ipykernel>=7.2.0",
        "matplotlib>=3.10.9",
        "networkx>=3.6.1",
        "numpy>=2.4.2",
        "pandas>=3.0.1",
        "python-dotenv>=1.0.0",
        "kaggle>=1.6.0",
        "scikit-learn>=1.8.0",
        "torch>=2.10.0",
        "torch-geometric>=2.7.0",
        "torch-scatter>=2.1.2",
        "torch-sparse>=0.6.18",
    ]

Version lower-bounds rationale:
- python-dotenv 1.0.0: stable API with load_dotenv() that downloader.py uses
- kaggle 1.6.0: supports the Kaggle API v1 used in downloader.py

Do not modify any other part of pyproject.toml (tool.uv section, requires-python, etc.).
  </action>

  <verify>
    <automated>python -c "
import tomllib
with open('pyproject.toml', 'rb') as f:
    data = tomllib.load(f)
deps = data['project']['dependencies']
names = [d.split('>=')[0].split('==')[0].strip() for d in deps]
assert 'python-dotenv' in names, f'FAIL: python-dotenv not in deps: {names}'
assert 'kaggle' in names, f'FAIL: kaggle not in deps: {names}'
print('PASS: both packages declared in pyproject.toml')
"
    </automated>
  </verify>

  <acceptance_criteria>
- `grep "python-dotenv" pyproject.toml` returns a line with version constraint
- `grep "kaggle" pyproject.toml` returns a line with version constraint (not a comment)
- `python -c "import tomllib; data=tomllib.load(open('pyproject.toml','rb')); print(data['project']['dependencies'])"` lists both packages without parse error
  </acceptance_criteria>

  <done>pyproject.toml lists python-dotenv and kaggle in [project] dependencies; uv sync / pip install -e . in a clean venv will install them</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → _download() | User-controlled --download flag triggers Kaggle API call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Information Disclosure | projects/.env (Kaggle credentials) | accept | .env is gitignored per D-03/D-04; credentials never committed. No change here. |
| T-01-04-02 | Tampering | pyproject.toml dependency versions | accept | Lower bounds only; no pinned exact versions. Acceptable for a research project. |
</threat_model>

<verification>
After both tasks complete, verify the full fix:

```bash
# 1. Import check — must not raise ImportError
python -c "
import sys; sys.path.insert(0, 'projects/src')
# Simulate the lazy import inside _download() without triggering actual download
import ast
src = open('projects/src/main.py').read()
assert 'download_elliptic_dataset' in src
assert 'dest_dir=' in src
print('main.py: import name and kwarg are correct')
"

# 2. Dependency check
python -c "
import tomllib
with open('pyproject.toml', 'rb') as f:
    data = tomllib.load(f)
deps = ' '.join(data['project']['dependencies'])
assert 'python-dotenv' in deps
assert 'kaggle' in deps
print('pyproject.toml: dependencies present')
"
```
</verification>

<success_criteria>
- `python projects/src/main.py --download` no longer raises `ImportError` or `TypeError` on the downloader import/call lines (runtime still needs .env with valid credentials, but the code path is correct)
- `pyproject.toml` declares python-dotenv and kaggle; `uv sync` in a clean env will install them
- INFRA-02 gap (CR-01, CR-02) fully closed
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-modularization/01-04-SUMMARY.md`
</output>
