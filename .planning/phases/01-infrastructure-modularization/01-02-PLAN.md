# Plan: 01-02 - Preprocessing & Dataset

**Objective:** Implement data preprocessing and the PyTorch Geometric dataset class.

---

## Frontmatter
- **Wave:** 2
- **Depends on:** 01-01
- **Files Modified:** 
  - `projects/src/data/preprocessor.py`
  - `projects/src/data/dataset.py`
- **Autonomous:** true
- **Requirements Addressed:** INFRA-03

---

## Tasks

<task>
<objective>Implement the data preprocessor.</objective>
<read_first>
- projects/Pattern_Project_Elliptic_dataset_GCN.ipynb
- projects/data/raw/elliptic_txs_features.csv
</read_first>
<acceptance_criteria>
- `projects/src/data/preprocessor.py` exists.
- Functions correctly merge `df_edge`, `df_class`, and `df_features`.
- "Unknown" nodes (class 3) are filtered.
- Features are normalized using `StandardScaler`.
</acceptance_criteria>
<action>
Extract preprocessing logic from cells 9-11 of the notebook. 
Implement a function that returns a cleaned `edge_index`, `x` (features), and `y` (labels).
Ensure it handles the `txId` to index mapping correctly.
</action>
</task>

<task>
<objective>Implement the EllipticDataset class.</objective>
<read_first>
- .planning/phases/01-infrastructure-modularization/01-RESEARCH.md
</read_first>
<acceptance_criteria>
- `projects/src/data/dataset.py` exists.
- Class inherits from `torch_geometric.data.InMemoryDataset`.
- `process()` method uses the preprocessor to build a `Data` object.
</acceptance_criteria>
<action>
Implement `EllipticDataset` class. 
The `Data` object should include `edge_index`, `x`, `y`, and `train_mask`/`test_mask` if applicable, or we handle splitting in the next plan.
For now, focus on constructing the full `Data` object.
</action>
</task>

---

## Verification
- Instantiate `EllipticDataset` and verify `data.x.shape` and `data.edge_index.shape`.
