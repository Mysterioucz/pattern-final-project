# Plan: 01-01 - Structure & Downloader

**Objective:** Set up the modular project structure and implement the Kaggle dataset downloader.

---

## Frontmatter
- **Wave:** 1
- **Depends on:** None
- **Files Modified:** 
  - `projects/src/data/downloader.py`
  - `projects/.env.example`
- **Autonomous:** true
- **Requirements Addressed:** INFRA-01, INFRA-02

---

## Tasks

<task>
<objective>Set up the projects directory structure.</objective>
<read_first>
- .planning/phases/01-infrastructure-modularization/01-RESEARCH.md
</read_first>
<acceptance_criteria>
- Directory `projects/src/data/` exists.
- Directory `projects/src/models/` exists.
- Directory `projects/src/utils/` exists.
- Directory `projects/data/raw/` exists.
</acceptance_criteria>
<action>
Run `mkdir -p projects/src/data projects/src/models projects/src/utils projects/data/raw projects/data/processed`.
</action>
</task>

<task>
<objective>Create .env.example with Kaggle keys.</objective>
<read_first>
- .planning/phases/01-infrastructure-modularization/01-CONTEXT.md
</read_first>
<acceptance_criteria>
- `projects/.env.example` contains `KAGGLE_USERNAME` and `KAGGLE_KEY`.
</acceptance_criteria>
<action>
Create `projects/.env.example` with:
```env
KAGGLE_USERNAME=your_username
KAGGLE_KEY=your_key
```
</action>
</task>

<task>
<objective>Implement the Kaggle dataset downloader.</objective>
<read_first>
- projects/Pattern_Project_Elliptic_dataset_GCN.ipynb
- .planning/phases/01-infrastructure-modularization/01-RESEARCH.md
</read_first>
<acceptance_criteria>
- `projects/src/data/downloader.py` exists.
- Script correctly reads `KAGGLE_USERNAME` and `KAGGLE_KEY` from `.env`.
- Script downloads `ellipticco/elliptic-data-set` to `projects/data/raw/`.
</acceptance_criteria>
<action>
Implement `downloader.py` using `python-dotenv` and the `kaggle` package. 
Reference the download logic from cell 8 of the notebook, but adapt it to save to `projects/data/raw/`.
</action>
</task>

---

## Verification
- Run `python projects/src/data/downloader.py` and verify files appear in `projects/data/raw/`.
