---
phase: 01-gcn-embedding-and-classical-classifiers-rf-xgboost-using-notebook-reference
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - tests/test_phase1_classical.py
autonomous: true
requirements:
  - P1-EMB-01
  - P1-EVAL-03
  - P1-XGB-04
must_haves:
  truths:
    - "Phase 1 has automated contract tests for embedding extraction and illicit-F1 behavior."
    - "XGBoost dependency is declared so the RF/XGB comparison can run in this repo."
  artifacts:
    - path: "tests/test_phase1_classical.py"
      provides: "Executable behavior contract tests for embedding/classifier pipeline"
      contains: "test_embedding_shape_and_determinism"
    - path: "pyproject.toml"
      provides: "xgboost dependency declaration"
      contains: "xgboost"
  key_links:
    - from: "tests/test_phase1_classical.py"
      to: "projects/src/models/gcn.py"
      via: "GCN.embed() contract checks"
      pattern: "embed"
    - from: "pyproject.toml"
      to: "uv.lock"
      via: "dependency lock sync"
      pattern: "xgboost"
---

<objective>
Create Wave 0 test + dependency foundation for Phase 1 so implementation work can be verified continuously.

Purpose: Prevent regressions while building the new GCN-embedding + RF/XGBoost pipeline.
Output: `tests/test_phase1_classical.py` and locked `xgboost` dependency.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-gcn-embedding-and-classical-classifiers-rf-xgboost-using-notebook-reference/01-CONTEXT.md
@.planning/phases/01-gcn-embedding-and-classical-classifiers-rf-xgboost-using-notebook-reference/01-RESEARCH.md
@projects/src/models/gcn.py
@projects/src/data/loader.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add XGBoost dependency to project environment</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - pyproject.toml
    - .planning/phases/01-gcn-embedding-and-classical-classifiers-rf-xgboost-using-notebook-reference/01-RESEARCH.md
  </read_first>
  <action>Add `xgboost` to `pyproject.toml` dependencies and refresh `uv.lock` using `uv add xgboost` (per research; no alternate booster libraries). Keep all existing dependency versions intact.</action>
  <verify>
    <automated>uv sync</automated>
  </verify>
  <acceptance_criteria>`xgboost` appears in both `pyproject.toml` and `uv.lock`, and `uv sync` completes without dependency resolution errors.</acceptance_criteria>
  <done>Environment can install XGBoost in a reproducible way for Phase 1 execution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write Phase 1 contract tests (embedding + RF/XGB + illicit-F1)</name>
  <files>tests/test_phase1_classical.py</files>
  <read_first>
    - tests/test_sampler.py
    - projects/src/models/gcn.py
    - .planning/phases/01-gcn-embedding-and-classical-classifiers-rf-xgboost-using-notebook-reference/01-VALIDATION.md
  </read_first>
  <behavior>
    - `test_embedding_shape_and_determinism`: `GCN.embed()` returns stable shape and values in eval/no_grad mode.
    - `test_rf_pipeline_smoke`: RF consumes concatenated raw+embedding features and returns predictions with matching length.
    - `test_xgb_pipeline_smoke`: XGB fit/predict path executes with sample weights and returns predictions with matching length.
    - `test_illicit_f1_contract`: illicit metric uses `f1_score(..., pos_label=0, average="binary")`.
  </behavior>
  <action>Create `tests/test_phase1_classical.py` with synthetic mini-graph fixtures and explicit assertions for each behavior above. Keep tests fast (&lt;60s total) and independent from the full Elliptic dataset.</action>
  <verify>
    <automated>pytest -q tests/test_phase1_classical.py -x</automated>
  </verify>
  <acceptance_criteria>All four contract tests exist and execute via pytest; failures clearly identify contract violations for embedding extraction, RF/XGB path, or illicit-F1 label orientation.</acceptance_criteria>
  <done>Phase 1 now has executable automated checks required for Nyquist-compliant implementation.</done>
</task>

</tasks>

<verification>
Run `pytest -q tests/test_phase1_classical.py -x` after each commit in this plan.
</verification>

<success_criteria>
- Dependency installation path includes XGBoost.
- Contract tests are present and runnable.
- No manual verification required for this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/01-gcn-embedding-and-classical-classifiers-rf-xgboost-using-notebook-reference/01-01-SUMMARY.md`
</output>
