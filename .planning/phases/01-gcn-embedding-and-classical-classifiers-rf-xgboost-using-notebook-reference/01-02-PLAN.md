---
phase: 01-gcn-embedding-and-classical-classifiers-rf-xgboost-using-notebook-reference
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - projects/src/utils/classical_pipeline.py
  - projects/src/utils/__init__.py
autonomous: true
requirements:
  - P1-EMB-01
  - P1-CLF-02
  - P1-EVAL-03
must_haves:
  truths:
    - "GCN embeddings are extracted through a reusable utility function using eval/no_grad."
    - "RF and XGB training paths both operate on raw+embedding concatenated features."
    - "Evaluation output includes illicit-F1 computed with pos_label=0."
  artifacts:
    - path: "projects/src/utils/classical_pipeline.py"
      provides: "Reusable embedding/classifier/evaluation functions for Phase 1"
      exports: ["extract_embeddings", "build_concat_features", "train_random_forest", "train_xgboost", "compute_metrics"]
    - path: "projects/src/utils/__init__.py"
      provides: "Re-export path for pipeline helpers"
      contains: "classical_pipeline"
  key_links:
    - from: "projects/src/utils/classical_pipeline.py"
      to: "projects/src/models/gcn.py"
      via: "model.embed(data)"
      pattern: "model\\.embed"
    - from: "projects/src/utils/classical_pipeline.py"
      to: "sklearn.metrics.f1_score"
      via: "compute_metrics() illicit F1"
      pattern: "pos_label=0"
---

<objective>
Implement tested reusable Phase 1 pipeline utilities (embedding extraction, feature concatenation, RF/XGB training, and metric computation).

Purpose: Separate core logic from CLI wiring for cleaner testing and reuse.
Output: `projects/src/utils/classical_pipeline.py` consumed by the training script plan.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-gcn-embedding-and-classical-classifiers-rf-xgboost-using-notebook-reference/01-RESEARCH.md
@.planning/phases/01-gcn-embedding-and-classical-classifiers-rf-xgboost-using-notebook-reference/01-01-SUMMARY.md
@tests/test_phase1_classical.py

<interfaces>
From projects/src/models/gcn.py:
```python
class GCN(nn.Module):
    def embed(self, data: Data) -> torch.Tensor: ...
```

From projects/src/data/loader.py:
```python
def get_static_split(normalize: bool = True, filter_unknown: bool = True) -> Data: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement core classical pipeline utility module</name>
  <files>projects/src/utils/classical_pipeline.py</files>
  <read_first>
    - tests/test_phase1_classical.py
    - projects/src/models/gcn.py
    - projects/src/train_kd.py
  </read_first>
  <behavior>
    - Embedding extraction runs with `model.eval()` + `torch.no_grad()` for deterministic features.
    - Feature matrix builder concatenates `data.x` and embeddings along feature axis.
    - RF trainer supports class-imbalance handling.
    - XGB trainer accepts sample_weight path.
    - Metrics include accuracy, precision, recall, f1_micro, f1_macro, f1_illicit(pos_label=0).
  </behavior>
  <action>Create `projects/src/utils/classical_pipeline.py` implementing pure functions for extraction, matrix assembly, RF/XGB fitting, and metric computation exactly matching Phase 1 research conventions (especially illicit label=0 semantics).</action>
  <verify>
    <automated>pytest -q tests/test_phase1_classical.py -x</automated>
  </verify>
  <acceptance_criteria>The full Phase 1 contract test file passes after implementation, with no fallback to notebook-only code paths.</acceptance_criteria>
  <done>Pipeline logic is reusable, tested, and ready to be wired into an executable script.</done>
</task>

<task type="auto">
  <name>Task 2: Export pipeline helpers from utils package</name>
  <files>projects/src/utils/__init__.py</files>
  <read_first>
    - projects/src/utils/__init__.py
    - projects/src/utils/classical_pipeline.py
  </read_first>
  <action>Update `projects/src/utils/__init__.py` to expose the new classical pipeline helper functions so script code can import from a stable package path.</action>
  <verify>
    <automated>python -c "from projects.src.utils.classical_pipeline import compute_metrics, extract_embeddings; print('ok')"</automated>
  </verify>
  <acceptance_criteria>Helper functions import cleanly through package paths without modifying `sys.path` hacks beyond existing project conventions.</acceptance_criteria>
  <done>Interface wiring for downstream script implementation is complete.</done>
</task>

</tasks>

<verification>
Re-run `pytest -q tests/test_phase1_classical.py -x` after code changes and before summary creation.
</verification>

<success_criteria>
- Contract tests for Phase 1 pipeline are green.
- Utility interfaces are importable and explicit.
- Illicit-F1 metric contract remains fixed at `pos_label=0`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-gcn-embedding-and-classical-classifiers-rf-xgboost-using-notebook-reference/01-02-SUMMARY.md`
</output>
