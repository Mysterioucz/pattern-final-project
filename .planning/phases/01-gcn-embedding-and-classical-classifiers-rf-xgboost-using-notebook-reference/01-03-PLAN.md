---
phase: 01-gcn-embedding-and-classical-classifiers-rf-xgboost-using-notebook-reference
plan: "03"
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - projects/src/train_gcn_classical.py
  - tests/test_phase1_classical.py
autonomous: true
requirements:
  - P1-EMB-01
  - P1-CLF-02
  - P1-EVAL-03
  - P1-XGB-04
must_haves:
  truths:
    - "A single script can run the full notebook-parity flow: GCN embeddings -> RF/XGB -> metrics."
    - "RF and XGB results are both emitted in a reproducible comparison output."
    - "Illicit-F1 is reported explicitly using label 0."
  artifacts:
    - path: "projects/src/train_gcn_classical.py"
      provides: "CLI entrypoint for Phase 1 pipeline"
      contains: "argparse"
    - path: "tests/test_phase1_classical.py"
      provides: "Script-level smoke checks updated for final wiring"
      contains: "phase1 script smoke"
  key_links:
    - from: "projects/src/train_gcn_classical.py"
      to: "projects/src/utils/classical_pipeline.py"
      via: "utility imports for extraction/training/evaluation"
      pattern: "from utils\\.classical_pipeline import"
    - from: "projects/src/train_gcn_classical.py"
      to: "projects/src/data/loader.py"
      via: "get_static_split() data loading"
      pattern: "get_static_split"
---

<objective>
Build and verify the executable Phase 1 entrypoint script that orchestrates the embedding + RF/XGB comparison workflow.

Purpose: Deliver the user-visible outcome of this phase (a runnable notebook-equivalent pipeline in `projects/src/`).
Output: `projects/src/train_gcn_classical.py` with automated smoke verification.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-gcn-embedding-and-classical-classifiers-rf-xgboost-using-notebook-reference/01-RESEARCH.md
@.planning/phases/01-gcn-embedding-and-classical-classifiers-rf-xgboost-using-notebook-reference/01-02-SUMMARY.md
@projects/src/train_kd.py
@projects/src/data/preprocessor.py
@projects/src/utils/classical_pipeline.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Phase 1 training script wiring</name>
  <files>projects/src/train_gcn_classical.py</files>
  <read_first>
    - projects/src/train_kd.py
    - projects/src/data/loader.py
    - projects/src/utils/classical_pipeline.py
    - .planning/phases/01-gcn-embedding-and-classical-classifiers-rf-xgboost-using-notebook-reference/01-RESEARCH.md
  </read_first>
  <behavior>
    - Script loads static split data and initializes/loads GCN for embedding extraction.
    - Script builds concatenated raw+embedding feature matrices.
    - Script trains RF and XGB, then reports both result rows.
    - Script prints illicit-F1 computed with `pos_label=0`.
  </behavior>
  <action>Create `projects/src/train_gcn_classical.py` with CLI flags for epoch/checkpoint/control options, reuse utility functions from `utils.classical_pipeline`, and keep implementation aligned with notebook behavior described in research (no DecisionTree branch, RF/XGB only).</action>
  <verify>
    <automated>python projects/src/train_gcn_classical.py --help</automated>
  </verify>
  <acceptance_criteria>CLI is executable, documents arguments, and contains full orchestration for loading data, extracting embeddings, training RF/XGB, and printing metrics.</acceptance_criteria>
  <done>Phase 1 has a runnable entrypoint for the classical classifier experiment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add script-level smoke coverage and finalize green checks</name>
  <files>tests/test_phase1_classical.py</files>
  <read_first>
    - tests/test_phase1_classical.py
    - projects/src/train_gcn_classical.py
  </read_first>
  <behavior>
    - Smoke test verifies script module imports and main wiring callable.
    - Metric output contract remains stable for illicit-F1 key.
  </behavior>
  <action>Extend `tests/test_phase1_classical.py` with lightweight smoke checks for script wiring (using monkeypatch/synthetic data where needed) so end-to-end orchestration is covered without full dataset training in CI.</action>
  <verify>
    <automated>pytest -q tests/test_phase1_classical.py -x</automated>
  </verify>
  <acceptance_criteria>All Phase 1 tests pass and include at least one smoke assertion against the new script orchestration path.</acceptance_criteria>
  <done>Phase 1 implementation is verifiable through automated tests and runnable CLI entrypoint.</done>
</task>

</tasks>

<verification>
- `pytest -q tests/test_phase1_classical.py -x`
- `pytest -q`
</verification>

<success_criteria>
- `projects/src/train_gcn_classical.py` exists and runs with `--help`.
- RF/XGB orchestration path is covered by automated tests.
- Full pytest suite remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/01-gcn-embedding-and-classical-classifiers-rf-xgboost-using-notebook-reference/01-03-SUMMARY.md`
</output>
