---
wave: 1
depends_on: []
files_modified:
  - projects/src/config.py
  - projects/src/models/base.py
  - projects/src/models/gcn.py
requirements_addressed:
  - MODEL-01
  - MODEL-02
autonomous: true
---

# Plan 02-01: Implement GCN Models (Teacher/Student)

**Objective**: Define the core GCN architectures for both Teacher and Student models, ensuring a modular and parameterizable implementation.

## Tasks

<task>
<id>02-01-01</id>
<objective>Create the project configuration file</objective>
<read_first>
- .planning/phases/02-model-implementation/02-CONTEXT.md
</read_first>
<action>
Create `projects/src/config.py` with the following parameters:
```python
class Config:
    # Model Params
    HIDDEN_CHANNELS = 64
    TEACHER_LAYERS = 2
    STUDENT_LAYERS = 1  # Default for 'Shallower'
    DROPOUT = 0.5
    
    # KD Params
    TEMPERATURE = 2.0
    ALPHA = 0.5
```
</action>
<acceptance_criteria>
- `projects/src/config.py` exists.
- Contains `TEMPERATURE = 2.0` and `ALPHA = 0.5`.
</acceptance_criteria>
</task>

<task>
<id>02-01-02</id>
<objective>Implement the Base GNN class</objective>
<read_first>
- projects/src/models/base.py (if exists)
</read_first>
<action>
Create `projects/src/models/base.py` defining a `BaseGNN` class inheriting from `torch.nn.Module`. It should provide a standard interface for forward passes.
</action>
<acceptance_criteria>
- `projects/src/models/base.py` exists.
- Contains `class BaseGNN(torch.nn.Module):`.
</acceptance_criteria>
</task>

<task>
<id>02-01-03</id>
<objective>Implement the Parameterized GCN class</objective>
<read_first>
- projects/src/models/base.py
- .planning/phases/02-model-implementation/02-CONTEXT.md
</read_first>
<action>
Create `projects/src/models/gcn.py` implementing the `GCN` class using `torch_geometric.nn.GCNConv`. 
The class must accept `num_layers` and `hidden_channels` in its constructor to allow for Teacher (2, 64) and Student (various) configurations.
Use `Config.DROPOUT` between layers.
</action>
<acceptance_criteria>
- `projects/src/models/gcn.py` exists.
- Uses `GCNConv` from `torch_geometric.nn`.
- Constructor accepts `num_layers`.
</acceptance_criteria>
</task>

## Verification

### Automated
- `pytest projects/src/tests/test_models.py::test_teacher_gcn`
- `pytest projects/src/tests/test_models.py::test_student_gcn`

### Manual
- None

## Must-Haves
- [ ] `GCN` class supports arbitrary number of layers.
- [ ] `Teacher` instantiation matches D-01 (2 layers, 64 units).
- [ ] `Student` variants can be instantiated from the same class.
